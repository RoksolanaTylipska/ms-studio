export enum ButtonType {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}

export enum ButtonSize {
  SMALL = "small",
  MEDIUM = "medium",
  LARGE = "large",
}

export enum ButtonColor {
  LIGHT = "light",
  DARK = "dark",
}