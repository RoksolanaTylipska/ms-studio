import { Box, Typography } from "@mui/material";

export default function Custom404() {
  return <Box style={{margin: '200px auto', textAlign: 'center'}}>
    <Typography variant="bodyCaveat">Сторінку не знайдено...</Typography>
  </Box>
}